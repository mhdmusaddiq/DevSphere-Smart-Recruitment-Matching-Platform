import { TestBed } from '@angular/core/testing';import { provideRouter } from '@angular/router';import { of } from 'rxjs';
import { EmployerApiService } from '../data-access/employer-api.service';import { EmployerContactRequestsComponent } from './employer-contact-requests.component';
describe('EmployerContactRequestsComponent',()=>{it('loads server contact requests',async()=>{const api=jasmine.createSpyObj('EmployerApiService',['getEmployerContactRequests','cancelContactRequest']);api.getEmployerContactRequests.and.returnValue(of([]));await TestBed.configureTestingModule({imports:[EmployerContactRequestsComponent],providers:[provideRouter([]),{provide:EmployerApiService,useValue:api}]}).compileComponents();TestBed.createComponent(EmployerContactRequestsComponent).detectChanges();expect(api.getEmployerContactRequests).toHaveBeenCalled();});});
