import { TestBed } from '@angular/core/testing';import { of } from 'rxjs';import { EmployerApiService } from '../data-access/employer-api.service';import { EmployerNotificationsComponent } from './employer-notifications.component';
describe('EmployerNotificationsComponent',()=>{it('loads notifications',async()=>{const api=jasmine.createSpyObj('EmployerApiService',['getNotifications','markNotificationRead']);api.getNotifications.and.returnValue(of([]));await TestBed.configureTestingModule({imports:[EmployerNotificationsComponent],providers:[{provide:EmployerApiService,useValue:api}]}).compileComponents();TestBed.createComponent(EmployerNotificationsComponent).detectChanges();expect(api.getNotifications).toHaveBeenCalled();});});
